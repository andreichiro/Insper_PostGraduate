name: ML Pipeline — Build, Test & Deploy

on:
  workflow_dispatch:
    inputs:
      pipeline:
        description: "Which pipeline to run"
        type: choice
        options: [full, modelling, refit]
        default: full
      deploy:
        description: "Deploy to Cloud Run after?"
        type: boolean
        default: true
      min_roc_auc:
        description: "Minimum roc_auc"
        type: string
        default: "0.75"
      min_f1:
        description: "Minimum F1 score"
        type: string
        default: "0.50"
      max_mape:
        description: "Maximum MAPE (%)"
        type: string
        default: "150.0"

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - run: uv sync --all-extras

      - name: Lint
        run: uv run ruff check src/ tests/

      - name: Unit tests
        run: uv run pytest --no-cov -x --ignore=tests/test_clean_data_hf.py

  train-and-validate:
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - run: uv sync --all-extras

      - name: Run Kedro pipeline
        run: |
          if [ "${{ inputs.pipeline }}" = "full" ]; then
            uv run kedro run
          else
            uv run kedro run --pipeline ${{ inputs.pipeline }}
          fi

      - name: Quality gate (all metrics)
        run: |
          uv run python - <<'PYEOF'
          import pickle, sys

          report = pickle.load(open("data/07_model_output/test_report.pkl", "rb"))

          thresholds = {
              "roc_auc": (">=", ${{ inputs.min_roc_auc }}),
              "f1":      (">=", ${{ inputs.min_f1 }}),
              "mape":    ("<=", ${{ inputs.max_mape }}),
          }

          print("=" * 65)
          print(f"{'Model':<12} {'roc_auc':>8} {'f1':>8} {'r2':>8} {'mape':>8}  {'CM'}")
          print("-" * 65)

          all_passed = True
          for name, m in report.items():
              cm = m.get("confusion_matrix", [])
              print(
                  f"{name:<12} {m['roc_auc']:>8.4f} {m['f1']:>8.4f} "
                  f"{m['r2']:>8.4f} {m['mape']:>7.2f}%  {cm}"
              )

          best = max(report.values(), key=lambda m: m["roc_auc"])
          print("=" * 65)
          print(f"Best model: roc_auc={best['roc_auc']:.4f}, f1={best['f1']:.4f}, "
                f"r2={best['r2']:.4f}, mape={best['mape']:.2f}%")
          print()

          failures = []
          for metric, (op, threshold) in thresholds.items():
              value = best[metric]
              if op == ">=" and value < threshold:
                  failures.append(f"{metric}={value:.4f} < {threshold}")
              elif op == "<=" and value > threshold:
                  failures.append(f"{metric}={value:.2f}% > {threshold}%")

          if failures:
              print("QUALITY GATE FAILED:")
              for f in failures:
                  print(f"  - {f}")
              sys.exit(1)
          else:
              print("QUALITY GATE PASSED")
          PYEOF

      - name: Save artifacts
        uses: actions/upload-artifact@v4
        with:
          name: model-artifacts
          path: |
            data/06_models/production_*.pkl
            data/07_model_output/test_report.pkl

  deploy:
    needs: train-and-validate
    if: ${{ inputs.deploy }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: model-artifacts
          path: data/

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Upload models to GCS
        run: gsutil -m cp data/06_models/production_*.pkl gs://${{ secrets.GCS_BUCKET }}/churn/models/

      - name: Build & deploy to Cloud Run
        run: |
          gcloud builds submit --tag gcr.io/${{ secrets.GCP_PROJECT }}/churn-api:${{ github.sha }}
          gcloud run deploy churn-api \
            --image gcr.io/${{ secrets.GCP_PROJECT }}/churn-api:${{ github.sha }} \
            --set-env-vars KEDRO_ENV=cloud,GCS_BUCKET=${{ secrets.GCS_BUCKET }},API_KEY=${{ secrets.API_KEY }} \
            --region us-central1 \
            --allow-unauthenticated \
            --memory 1Gi
